<%= render 'shared/deletion_request_confirm',
            record: @theme,
            deletion_request: @themes_deletion_request,
            content_type: 'お題',
            create_path: theme_themes_deletion_requests_path(@theme) %>