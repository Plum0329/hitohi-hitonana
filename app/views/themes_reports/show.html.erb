<%= render 'shared/report_detail',
            record: @themes_report.theme,
            report: @themes_report,
            content_type: "お題",
            return_path: theme_path(@themes_report.theme) %>